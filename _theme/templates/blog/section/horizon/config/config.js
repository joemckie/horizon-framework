jQuery(document).ready(function($){
	$(".slidecontrol").change(function(){
		var val = $(this).val();
		var id = $(this).attr("id");
		
		var style = ''; // this must correspond to the style chosen
		
		// Use this file to show and hide meta boxes based on the select box value
	});
});